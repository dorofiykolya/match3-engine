<UserControl x:Class="Match3.Editor.LevelEditor.LevelEditorAvailableItems"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Match3.Editor.LevelEditor"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" Width="300">
    <materialDesign:Card Background="{DynamicResource PrimaryHueDarkBrush}"
                  Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}"
                  Width="300"
                  Padding="8">
        <StackPanel>
            <TextBlock TextAlignment="Center" Margin="5" FontSize="10">Generate Items</TextBlock>
            <Separator Style="{StaticResource MaterialDesignLightSeparator}"/>
            <WrapPanel Name="Items" Orientation="Horizontal" Margin="8 0 8 8" HorizontalAlignment="Center">
                
            </WrapPanel>
        </StackPanel>
    </materialDesign:Card>
</UserControl>
