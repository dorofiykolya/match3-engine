<Button x:Class="Match3.Editor.Player.SpellItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Match3.Editor.Player"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Padding="0"
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             Width="50" Height="50">
    <Grid>
        <materialDesign:PackIcon Kind="CheckboxBlankCircleOutline" Width="{Binding Width}" Height="{Binding Height}"></materialDesign:PackIcon>
        <Label HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Content="{Binding Label}" Width="{Binding Width}" Height="{Binding Height}"></Label>
    </Grid>
</Button>
